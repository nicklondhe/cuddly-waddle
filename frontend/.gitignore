# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Don't exclude the puzzles directory structure but ignore its contents
# This allows git to create the directory when cloned but not commit the puzzle files themselves
# Uncomment these lines if you want to pre-generate puzzles but not commit them
# /public/puzzles/*.json
# !/public/puzzles/.gitkeep
